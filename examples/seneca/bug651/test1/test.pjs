void setup() {
  size(100, 100);

  int i = frameRate;

  tinylogLite.log(frameRate);
  tinylogLite.log("FPS:" + frameRate);
  tinylogLite.log((frameRate));
  tinylogLite.log((frameRate*2));
  tinylogLite.log(frameRate + func());
  tinylogLite.log(frameRate+(42));
  tinylogLite.log(i);
  tinylogLite.log(FR());
  tinylogLite.log(">" + frameRate + "<");  
}

int func(){
  return 42;
}

int FR(){
  return frameRate;
}